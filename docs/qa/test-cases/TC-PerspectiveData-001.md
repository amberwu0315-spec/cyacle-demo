# TC-PerspectiveData-001

## 用例信息
- 用例ID：TC-PerspectiveData-001
- 页面/模块：PerspectiveData
- 用例标题：核算 L3 透视分组支持活动数据与因子数据切换
- 优先级：P0
- 前置条件：
  - 应用可正常启动
  - 当前账号可进入 `项目标签 -> 核算`
- 测试数据：无需新增数据

## 测试步骤
1. 在核算 L3 侧栏 `透视` 分组点击 `活动数据`，记录当前标题。
2. 再点击 `因子数据`，记录当前标题。
3. 再次切回 `活动数据`。

## 预期结果
1. `透视` 分组两个入口均可点击切换，无报错。
2. 两个页面标题可区分（`Activity Data (透视)` 与 `Factor Data (透视)`）。
3. 来回切换后页面状态稳定。

## 结果记录
- 执行日期：2026-02-14
- 执行人：Codex（结构回归）
- 执行结果：Pass
- 缺陷链接：
- 备注：透视分组切换链路正常。
