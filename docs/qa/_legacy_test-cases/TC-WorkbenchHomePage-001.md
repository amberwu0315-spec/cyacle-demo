# TC-WorkbenchHomePage-001

## 用例信息
- 用例ID：`TC-WorkbenchHomePage-001`
- 页面/模块：`WorkbenchHomePage`
- 用例标题：工作台卡片导航链路可达
- 优先级：`P0`
- 前置条件：
  - 应用可正常启动
  - 当前账号可进入 `L1 工作空间 -> L2 工作台`
- 测试数据：无需新增数据

## 测试步骤
1. 进入 `工作空间 -> 工作台` 页面。
2. 点击上方能力卡片 `元件`。
3. 返回工作台后点击能力卡片 `组织碳盘查`。
4. 返回工作台后点击能力卡片 `园区碳足迹`。

## 预期结果
1. 工作台页面展示正常，卡片可点击。
2. 点击 `元件` 后进入 `L1 背景数据 -> L2 元件`。
3. 点击 `组织碳盘查` 后进入 `L1 项目管理 -> 组织碳足迹`。
4. 点击 `园区碳足迹` 后进入 `L1 服务企业 -> 全部服务企业`。

## 结果记录
- 执行日期：2026-02-14
- 执行人：Codex（Playwright UI 回归）
- 执行结果：`Pass`
- 缺陷链接：
- 备注：三条卡片导航链路均通过；同时校验 URL 参数与目标页面状态一致。
