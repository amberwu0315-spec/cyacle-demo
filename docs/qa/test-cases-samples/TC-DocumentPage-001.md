# TC-DocumentPage-001

## 用例信息
- 用例ID：`TC-DocumentPage-001`
- 页面/模块：`DocumentPage`
- 用例标题：列表双击进入主从详情并返回，创建态可进入退出
- 优先级：`P0`
- 前置条件：
  - 应用可正常启动
  - 当前账号可进入 `L1 服务企业 -> L2 文档`
- 测试数据：页面有至少 1 条文档数据

## 测试步骤
1. 进入 `服务企业 -> 文档` 列表页。
2. 双击任意一行文档数据。
3. 在主从详情页右栏顶部点击 `返回列表`。
4. 在列表页点击 `创建`，进入创建页后点击 `取消`。

## 预期结果
1. 文档列表默认展示正常，且支持双击交互。
2. 成功进入主从详情页，左栏为列表、右栏为详情。
3. 点击 `返回列表` 后回到列表页，且无报错。
4. 成功进入创建态并可返回列表页，流程无阻塞。

## 结果记录
- 执行日期：2026-02-14
- 执行人：Codex（Playwright UI 回归）
- 执行结果：`Pass`
- 缺陷链接：
- 备注：文档页列表/双击主从/返回列表/创建态进入与退出链路均通过。
